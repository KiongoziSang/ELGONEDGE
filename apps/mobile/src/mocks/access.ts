import type { AccessInfo } from "../types";

export const accessInfo: AccessInfo = {
  accessCardStatus: "Active access card on file",
  unitAccessInfo: "Unit B-204 resident access is active for Grace Wanjiku.",
  visitorPreRegistration: "Visitor pre-registration is available depending on property setup.",
  gatePassStatus: "No active gate pass found"
};
